{
  "name": "fullstackgovitejs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "tsc -b && vite build --mode development && vite preview",
    "test": "vitest run",
    "watch": "vitest watch"
  },
  "dependencies": {
    "@preact/signals": "^1.3.0",
    "preact": "^10.23.1",
    "preact-iso": "^2.6.3",
    "supertokens-auth-react": "^0.46.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "@testing-library/preact": "^3.2.4",
    "happy-dom": "^15.0.0",
    "msw": "^2.3.5",
    "typescript": "^5.5.3",
    "vite": "^5.4.1",
    "vitest": "^2.0.5"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "^4.21.1"
  }
}
